// Make a program that filters a list of strings and returns a list with only your friends name in it.
// If a name has exactly 4 letters in it, they are your friend, otherwise they are not. 


function friend(friends) {
  return myFriends = friends.filter(friend => friend.length === 4)
}